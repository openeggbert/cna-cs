namespace Microsoft.Xna.Framework.Audio;

/// <summary>XNA 4.0-compatible <c>AudioListener</c>. A pure subclass -- every member is a
/// <see cref="Vector3"/>, which converts implicitly across the boundary, so nothing needs
/// re-typing.</summary>
public class AudioListener : CNA.Audio.AudioListener
{
}
