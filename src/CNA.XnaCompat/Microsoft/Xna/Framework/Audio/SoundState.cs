namespace Microsoft.Xna.Framework.Audio;

/// <summary>See CNA.Audio.SoundState; values kept numerically identical to it.</summary>
public enum SoundState
{
    Playing = 0,
    Paused = 1,
    Stopped = 2,
}
